import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ConversorMoedas.frontend',
  webDir: 'www'
};

export default config;
